# Optional: set these to use real LLM providers instead of the built-in mock provider.
# Leave blank to run the full platform (including the seeded demo) for free, offline.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
